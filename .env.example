# General Settings
SECRET_KEY=replace-with-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Database Configuration (PostgreSQL / MySQL / SQLite)
# Para PostgreSQL: postgresql://usuario:contraseña@localhost:5432/ventinv_db
# Para MySQL: mysql://usuario:contraseña@localhost:3306/ventinv_db
# Si DATABASE_URL está vacía, utilizará SQLite3 local por defecto.
DATABASE_URL=
